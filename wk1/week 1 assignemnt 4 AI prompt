Here's an even more literal version — basically dictating every line so there's no interpretation left, just transcription into a formatted document.

Prompt:

Write out the following Java lab as a complete markdown document. Follow these instructions exactly, treating each one as a literal dictation of what to type — do not reinterpret, rename, or restructure anything.

Write a header "Part 1: Plan the Program Using IPO". Underneath, write "Input:" followed by a list of these five items exactly: distance, milesPerGallon, pricePerGallon, averageSpeed, numberOfTravelers. Then write "Process:" followed by these four lines exactly: "gallonsNeeded = distance / milesPerGallon", "fuelCost = gallonsNeeded * pricePerGallon", "travelHours = distance / averageSpeed", "costPerTraveler = fuelCost / numberOfTravelers". Then write "Output:" followed by these six items exactly: gallonsNeeded, fuelCost, travelHours, wholeHours, remainingMinutes, costPerTraveler.
Write a header "Part 2: Create the Java Program". In a fenced java code block, type exactly: import java.util.Scanner; then a blank line, then public class TripCostAnalyzer { then an indented blank line, then public static void main(String[] args) { then an indented blank line for Scanner input = new Scanner(System.in); then close both braces.
Write a header "Part 3: Declare a Constant". In the code block, inside main, type exactly: final int MINUTES_PER_HOUR = 60; and add a note underneath saying this constant will be reused later instead of typing 60 again.
Write a header "Part 4: Read User Input". In the code block, type five pairs of lines: a System.out.print statement with the prompt text, followed by a variable declaration reading from input. Type them in this exact order: "Enter trip distance in miles: " reading into double distance; "Enter vehicle miles per gallon: " reading into double milesPerGallon; "Enter fuel price per gallon: " reading into double pricePerGallon; "Enter average driving speed: " reading into double averageSpeed; "Enter number of travelers: " reading into int numberOfTravelers.
Write a header "Part 5: Calculate Fuel Usage and Cost". Type exactly: double gallonsNeeded = distance / milesPerGallon; then double fuelCost = gallonsNeeded * pricePerGallon; then two println statements printing "Gallons needed: " + gallonsNeeded and "Fuel cost: " + fuelCost.
Write a header "Part 6: Calculate Travel Time". Type exactly: double travelHours = distance / averageSpeed;.
Write a header "Part 7: Convert Decimal Hours into Hours and Minutes". Type exactly: int wholeHours = (int) travelHours; then double fractionalHour = travelHours - wholeHours; then int remainingMinutes = (int) (fractionalHour * MINUTES_PER_HOUR); then a println statement printing "Estimated driving time: " + wholeHours + " hours and " + remainingMinutes + " minutes".
Write a header "Part 8: Calculate Cost Per Traveler". Type exactly: double costPerTraveler = fuelCost / numberOfTravelers; then a println printing "Fuel cost per traveler: " + costPerTraveler.
Write a header "Part 9: Sample Run". State the exact input values used: 360, 30, 4.50, 60, 3. State the exact output produced, line by line: "Gallons needed: 12.0", "Fuel cost: 54.0", "Travel time: 6.0 hours", "Estimated driving time: 6 hours and 0 minutes", "Fuel cost per traveler: 18.0". Write a sentence confirming these numbers match.
Write a header "Part 10: Intermediate Challenge". Type a new prompt/read pair: "Enter additional parking or toll cost: " reading into double additionalCost. Type exactly: double totalTripCost = fuelCost + additionalCost; then replace the costPerTraveler line with double costPerTraveler = totalTripCost / numberOfTravelers;. Type two separate println statements printing fuelCost and totalTripCost with clear labels.
Write a header "Part 11: Remainder Operator Challenge". In a separate fenced code block, type exactly: int totalMinutes = 335; then int hours = totalMinutes / 60; then int minutes = totalMinutes % 60; then a println printing hours + " hours and " + minutes + " minutes". State the output is exactly "5 hours and 35 minutes".
Write a header "Part 12: Debugging Challenge". In a fenced code block, type exactly: int distance = 350; then int speed = 80; then double travelTime = distance / speed; then System.out.println(travelTime);. Write a sentence stating the predicted output is 4.0. Then write out this exact explanation in full sentences: the fractional portion is lost because Java performs the division using the int types first, truncating 350/80 down to 4 before that value is ever converted to a double, so the decimal information never existed to begin with. Write that the fix is to cast only one operand — write the corrected line exactly as double travelTime = (double) distance / speed; — and state the corrected output is 4.375.
Write a header "Part 13: Code Quality Review". Write out this exact checklist as confirmed statements: variable names are meaningful; the class name TripCostAnalyzer follows PascalCase convention; the constant MINUTES_PER_HOUR is uppercase with underscores; only one Scanner object is created; double is used wherever decimal precision matters and int only for guaranteed whole numbers; no calculation divides two ints where a decimal result is expected; every printed line is clearly labeled; indentation is consistent throughout.
Write a header "Check Your Understanding". Answer each of these seven questions using the exact reasoning given: double is better than int for fuel price because prices include cents and int cannot store any fraction. numberOfTravelers is an int because a person cannot be fractional. Casting a double to int truncates the decimal completely, it does not round. Integer division can be wrong when assigned to a double because Java computes the division using the original int types before the result is ever converted, so the decimal is already lost. The remainder operator is useful in time conversion because it directly produces the leftover units after removing whole units, such as leftover minutes after removing whole hours. A named constant is better than a repeated literal because it improves readability and only needs to be updated in one place if the value changes. The IPO model helps by separating what data is needed, what will be done with it, and what the user should see, before writing any code.
